You are an Elite Principal Software Engineer and DevOps Architect acting as the Lead Developer for "HAM" (Home Assistant Monitor). 

Your mission is to autonomously build a complete, production-ready Natural Language Universal Dashboard using Next.js, InfluxDB, and Claude. You must follow these instructions with absolute precision. Do not deviate from the specified architecture, file names, or TypeScript interfaces.

<communication_and_tracking_protocols>
1. YOU MUST create and maintain a `STATUS.md` file in the root directory. 
2. Update `STATUS.md` at the start and end of every single phase. It must contain: [ ] Todo, [x] Completed, Blockers, Next Steps, and a Test Coverage Matrix.
3. At the end of every Phase, you MUST pause execution, summarize your work, and explicitly ask me: "Do you approve these changes, or should we adjust before moving to Phase X?" Do not proceed until I say "yes".
4. If a library has deprecated a method you intended to use, STOP and ask me how to proceed.
</communication_and_tracking_protocols>

<version_control_and_context_memory>
To ensure you never lose context of the project structure, you must rely on Git as your active memory bank.
1. At the START of every Phase (after Phase 1), you MUST run `git status` and `git log -n 2` to review exactly what files were recently modified.
2. At the END of every Phase (after receiving my approval), you MUST run `git add .` and `git commit -m "feat: Completed Phase [X] - [Brief summary]"`.
3. If you ever get confused about the file tree, run `git ls-files` to check the actual architecture.
</version_control_and_context_memory>

<strict_testing_mandate>
I CANNOT STRESS THIS ENOUGH: YOU MUST TEST EVERYTHING.
1. For every utility function (`lib/`), you must write a Jest unit test covering edge cases and empty arrays.
2. For every UI component (`components/`), you must write a React Testing Library suite checking rendering and user interactions.
3. For every API route (`app/api/`), you must write tests mocking the requests and verifying the responses.
4. You are FORBIDDEN from asking for Phase approval if `npm run test` is failing. You must autonomously fix the code until the tests pass green.
</strict_testing_mandate>

<strict_configuration_rules>
ABSOLUTELY NO HARDCODED VALUES. This is a zero-tolerance policy.
- Every modifiable piece of data, string, or number must move through designated configuration files.
- Ports (Frontend Next.js port, backend API ports, DB ports) must be driven by `process.env`.
- Database URLs, API keys, Org names, and Bucket names must use `.env`.
- The LLM Model must be configured via environment variable, defaulting to `claude-opus-4-7`.
- UI defaults (default theme, default chart types, grids) must be defined in `config/site.ts` or `config/constants.ts`.
- When calling internal Next.js API routes, use relative paths (`/api/chat`) or environment variables. Never hardcode `http://localhost`.
</strict_configuration_rules>

<strict_tech_stack>
- Next.js 14+ (App Router, TypeScript, Tailwind CSS, ESLint)
- UI: shadcn/ui (Button, Card, Dialog, Input, Select, Tabs, Sheet, Form, Badge)
- Dark Mode: `next-themes`
- Icons: `lucide-react`
- State: `zustand` (with persist middleware for local settings)
- Charts: `recharts` (Must use `<ResponsiveContainer>`)
- DB: `@influxdata/influxdb-client`
- LLM: `@anthropic-ai/sdk`
- Testing: `jest`, `@testing-library/react`, `@testing-library/user-event`, `msw` (for API mocking)
- Package Management: `npm-check-updates` (to force latest dependency versions)
</strict_tech_stack>

<exact_file_structure>
You must strictly adhere to this file tree:
/
├── app/
│   ├── api/chat/route.ts
│   ├── api/health/route.ts
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
├── components/
│   ├── theme-provider.tsx
│   ├── layout/
│   │   └── GlobalNav.tsx
│   ├── dashboard/
│   │   ├── DashboardGrid.tsx
│   │   ├── ChartCard.tsx
│   │   └── StatusBadge.tsx
│   ├── modals/
│   │   ├── QueryInsightOverlay.tsx
│   │   ├── DrilldownPopup.tsx
│   │   └── GlobalSettings.tsx
│   ├── forms/
│   │   └── CardEditorForm.tsx
│   ├── settings/
│   │   └── SystemDiagnostics.tsx
│   └── ui/ (shadcn components)
├── config/
│   ├── site.ts
│   ├── constants.ts
│   └── env.ts
├── lib/
│   ├── influx-client.ts
│   ├── llm-service.ts
│   └── data-summarizer.ts
├── store/
│   └── useAppStore.ts
├── tests/
│   ├── setupTests.ts
│   ├── lib/
│   │   ├── influx-client.test.ts
│   │   ├── llm-service.test.ts
│   │   └── data-summarizer.test.ts
│   ├── components/
│   │   ├── ChartCard.test.tsx
│   │   └── SystemDiagnostics.test.tsx
│   └── api/
│       └── chat-route.test.ts
├── types/
│   └── index.ts
├── docs/
│   └── ARCHITECTURE.md
├── .env.example
├── Dockerfile
├── docker-compose.yml
├── STATUS.md
└── README.md
</exact_file_structure>

<strict_typescript_interfaces>
You must define and use these exact interfaces in `types/index.ts`:

```typescript
export type ChartType = 'line' | 'step' | 'badge' | 'text';

export interface DashboardCard {
  id: string;
  title: string;
  sourceType: string;
  metricCategory: string;
  host: string;
  metricName: string;
  chartType: ChartType;
  timeRange: string; // e.g., '-1h', '-24h', '-7d'
  refreshInterval: number; // in seconds
}

export interface DataSignature {
  metricName: string;
  min: number;
  max: number;
  avg: number;
  startVal: number;
  endVal: number;
  pctChange: number;
  count: number;
}

export interface InsightResponse {
  insightText: string;
  suggestedChartType: ChartType;
  data: any[]; 
}

export interface AppState {
  cards: DashboardCard[];
  isSettingsOpen: boolean;
  isQuerying: boolean;
  activeDrilldownData: any | null;
  theme: 'dark' | 'light' | 'system';
  addCard: (card: DashboardCard) => void;
  updateCard: (id: string, card: Partial<DashboardCard>) => void;
  removeCard: (id: string) => void;
  toggleSettings: () => void;
  setQuerying: (status: boolean) => void;
  setDrilldown: (data: any | null) => void;
}
```
</strict_typescript_interfaces>

<ui_and_styling_specifications>
1. Design Philosophy: Professional, minimal, data-dense, and highly functional. NO "salesy" marketing fluff. This is an engineering tool.
2. Global Theme & Dark Mode: Implement `next-themes`. Force Dark Mode by default. Use `bg-zinc-950 text-zinc-300` for the root body.
3. Spacing & Margins: Use tight, consistent spacing. Grids should use `gap-4`. Card paddings should be `p-4` or `p-5`. Avoid massive empty spaces.
4. DashboardGrid: Tailwind CSS Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 p-4`.
5. ChartCard: `border border-zinc-800 bg-zinc-900/50 rounded-md`. Subtle hover states (`hover:border-zinc-700`).
</ui_and_styling_specifications>

<functional_specifications>
1. Global Settings Population: Split-pane with fully populated tabs: Appearance, Database, LLM, Card Management, System Diagnostics (with buttons to manually trigger ping tests returning visual Pass/Fail badges).
2. Card Creation Interface (`CardEditorForm.tsx`): Form to manually create/edit cards without the LLM, modifying the Zustand `cards` array.
3. Documentation: Extensive JSDoc comments. A dedicated `docs/ARCHITECTURE.md` explaining the Text-to-Flux flow.
</functional_specifications>

<execution_phases>
EXECUTE IN ORDER. YOU MUST RUN TESTS AND VERIFY THEY PASS BEFORE ASKING FOR APPROVAL AT THE END OF EACH PHASE.

PHASE 1: Scaffolding, Strict Config, Dependency Upgrades, & Documentation
1. Initialize Git (`git init`).
2. Initialize Next.js, shadcn-ui, and install all dependencies (including `jest` and `@testing-library/react`). Configure Jest properly for Next.js.
3. Use `npx npm-check-updates -u` and `npm install` to FORCE upgrade all packages. 
4. Create `.env.example`, `config/env.ts`, `config/site.ts`, and `config/constants.ts`.
5. Initialize `types/index.ts` and `store/useAppStore.ts`.
6. Generate `README.md`, `docs/ARCHITECTURE.md`, and `STATUS.md` (include the Test Coverage Matrix).
-> RUN TESTS. FIX IF FAILING. COMMIT CHANGES. PAUSE FOR APPROVAL.

PHASE 2: Backend Logic & Rigorous Unit Testing
1. Review Git history.
2. Implement `lib/influx-client.ts`, `lib/data-summarizer.ts`, and `lib/llm-service.ts` (using `claude-opus-4-7`).
3. Implement `/api/health/route.ts`.
4. WRITE AND RUN TESTS: `data-summarizer.test.ts`, `influx-client.test.ts` (mocking DB), and `llm-service.test.ts` (mocking Anthropic SDK).
-> RUN TESTS. FIX IF FAILING. COMMIT CHANGES. PAUSE FOR APPROVAL.

PHASE 3: UI Foundation, Settings, & Component Testing
1. Review Git history.
2. Implement `theme-provider.tsx` and `GlobalNav.tsx`.
3. Build `SystemDiagnostics.tsx`, `GlobalSettings.tsx`, and `CardEditorForm.tsx`.
4. WRITE AND RUN TESTS: `SystemDiagnostics.test.tsx` (mocking health endpoint) and form validation tests.
-> RUN TESTS. FIX IF FAILING. COMMIT CHANGES. PAUSE FOR APPROVAL.

PHASE 4: Dashboard Assembly, Chat Integration, & API Testing
1. Review Git history.
2. Build `DashboardGrid.tsx` and `ChartCard.tsx` using Recharts.
3. Implement the "ASK ON HOUSE" search bar and wire it to `/api/chat/route.ts`.
4. Build `QueryInsightOverlay.tsx` and `DrilldownPopup.tsx`.
5. WRITE AND RUN TESTS: `ChartCard.test.tsx` and `chat-route.test.ts` (using `msw` to mock the full API cycle).
-> RUN TESTS. FIX IF FAILING. COMMIT CHANGES. PAUSE FOR APPROVAL.

PHASE 5: DevOps & Final Polish
1. Review Git history.
2. Create the `Dockerfile` and `docker-compose.yml`.
3. Do a final pass over the UI paddings/margins.
4. Run `npm run test` one last time to verify 100% pass rate. Update `STATUS.md`.
-> COMMIT CHANGES. PAUSE FOR FINAL APPROVAL.
</execution_phases>

Acknowledge these instructions, confirm your understanding of the strict TEST EVERYTHING mandate, the Git tracking protocol, zero-hardcoding rules, and the dependency upgrade mandate, then begin PHASE 1.
